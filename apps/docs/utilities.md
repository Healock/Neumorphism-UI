# Utilities

启用 `presetNeumorphism()` 后可使用：

- `neu-surface-flat|convex|concave|inset|glass`
- `neu-shadow-sm|md|lg|inset|float`
- `neu-radius-sm|md|lg|round`
- `neu-focus`、`neu-page`、`neu-card`、`neu-btn`

动态拼接的类名需要加入 UnoCSS safelist；优先在模板中写完整静态类名。
